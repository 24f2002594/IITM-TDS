name: Email Verification Action

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  verify-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: 24f2002594@ds.study.iitm.ac.in
        run: |
          echo "Executing run for 24f2002594@ds.study.iitm.ac.in"
          echo "Verification Step Completed Successfully"
